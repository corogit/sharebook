<h1>BOOK一覧</h1>
<% @books.each do |book| %>
  <%= link_to book_path(book.id) do %> 
    <%= book.title %>
  <% end %>
    <%= book.author %>
    <%= book.body %>
<% end %>
<%= link_to "新規投稿", new_book_path %>